# Not read by the app yet — src/api/registrationApi.ts is a local-only
# placeholder until a real backend exists (see the comment at the top of
# that file). Once one does, wire these in and point registrationApi.ts's
# functions at it.
# VITE_API_BASE_URL=
# VITE_EVENT_ID=
